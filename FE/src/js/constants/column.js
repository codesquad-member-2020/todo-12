export const controllerInfo = {
    initialUrl: "http://15.165.163.174:8080",
    option: {
        cardFocus: true,
        inputEvent: true,
        dblclickEvent: true,
    }
}

export const modelInfo = {
    selector: {
        card: ".column__card"
    }
}

export const viewInfo = {
    selector: {
        columnArea: "#todo",
        cardArea: ".column__cards",
        card: ".column__card",
        column: ".todo__column",
        borderFocus: "border-focus",
        cardContent: ".card__content",
        columnTitle: ".column__title",
        columnCount: ".column__card-count",
    }
}